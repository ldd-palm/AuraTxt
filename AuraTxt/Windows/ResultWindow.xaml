<Window x:Class="AuraTxt.Windows.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        Width="480" MinHeight="120" ResizeMode="CanResize"
        KeyDown="Window_KeyDown">
  <Border Background="#1E1E1E" CornerRadius="8" Margin="8">
    <Border.Effect>
      <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.4" Color="Black"/>
    </Border.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="36"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <Border Grid.Row="0" Background="#2A2A2A" CornerRadius="8,8,0,0"
              MouseLeftButtonDown="TitleBar_MouseDown">
        <Grid Margin="10,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <Button Grid.Column="0" Width="12" Height="12" Background="#FF5F57"
                  BorderBrush="Transparent"
                  Template="{StaticResource CircleButtonTemplate}"
                  Click="CloseBtn_Click"/>
          <TextBlock Grid.Column="1" x:Name="TitleLabel" Foreground="#888" FontSize="11"
                     VerticalAlignment="Center" Margin="8,0" TextTrimming="CharacterEllipsis"/>
          <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,0">
            <Button Width="26" Height="26" Style="{StaticResource IconBtnStyle}"
                    ToolTip="编辑 Prompt (P)" Click="EditBtn_Click" Content="✏️"/>
            <Button Width="26" Height="26" Style="{StaticResource IconBtnStyle}"
                    ToolTip="重新生成 (R)" Click="RegenBtn_Click" Content="🔄" Margin="4,0,0,0"/>
            <Button Width="26" Height="26" Background="#6366F1" BorderBrush="#6366F1"
                    Foreground="White" ToolTip="复制全部 (C)" Click="CopyBtn_Click"
                    Content="📋" Margin="4,0,0,0"/>
          </StackPanel>
        </Grid>
      </Border>

      <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="4">
        <TextBox x:Name="ResultText" Background="Transparent" Foreground="#E2E8F0"
                 IsReadOnly="True" TextWrapping="Wrap" BorderThickness="0"
                 Padding="12" AcceptsReturn="True" FontFamily="Segoe UI"/>
      </ScrollViewer>
    </Grid>
  </Border>
</Window>
