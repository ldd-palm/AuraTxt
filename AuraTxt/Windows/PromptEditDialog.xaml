<Window x:Class="AuraTxt.Windows.PromptEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑 Prompt" Width="480" Height="280"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="#E2E8F0">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBox x:Name="PromptBox" Grid.Row="0"
             Background="#0F172A" Foreground="#E2E8F0" BorderBrush="#334155"
             AcceptsReturn="True" TextWrapping="Wrap"
             FontFamily="Consolas" FontSize="13" Padding="8"
             VerticalScrollBarVisibility="Auto"/>
    <StackPanel Grid.Row="1" Orientation="Horizontal"
                HorizontalAlignment="Right" Margin="0,8,0,0">
      <Button Content="取消" Width="70" Height="28" Margin="0,0,8,0" Click="Cancel_Click"/>
      <Button Content="确认生成" Width="80" Height="28"
              Background="#6366F1" Foreground="White" BorderBrush="Transparent"
              Click="OK_Click"/>
    </StackPanel>
  </Grid>
</Window>
